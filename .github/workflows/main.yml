name名称: 每日自动签到

# 触发条件
on:启用:
  schedule:
    # 每天北京时间 09:00 运行 (对应 UTC 01:00)
    - cron: '0 1 * * *'
  # 允许手动点击按钮运行
  workflow_dispatch:

jobs:作业:
  checkin:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉取代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 设置 Node.js 环境
      - name: Use Node.js      - 名称: 使用 Node.js      - name: 使用 Node.js      - 名称: 使用 Node.js
        uses: actions/setup-node@v4
        with:        使用:
          node-version: '20'          node-version: '20'          node-version: '20'          node-version: '20'
          cache: 'npm'

      # 3. 安装依赖
      - name: Install dependencies      - name: 安装依赖
        run: npm install        运行: npm install

      # 4. 运行脚本
      - name: Run Script      - name: 运行脚本      - 名称: 运行脚本      - 名称: 运行脚本
        env:        环境:
          KLW_USERNAME: ${{ secrets.KLW_USERNAME }}
          KLW_PASSWORD: ${{ secrets.KLW_PASSWORD }}
        run: node index.js        运行:         运行: node index.js        运行: node index.js
